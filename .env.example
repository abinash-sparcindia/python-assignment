# Optional overrides for the review stack; Compose works without this file.
# Never commit real credentials or signing keys.
APP_PORT=8000
CORS_ORIGINS=http://localhost:3000
TOKEN_LIFETIME_MINUTES=60
REQUESTS_PER_MINUTE=60
# In non-review deployments, supply SIGNING_SECRET outside version control.
# The Compose review stack generates one at startup.
